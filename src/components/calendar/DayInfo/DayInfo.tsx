import './dayInfo.css';
import DayInfoHead from '../DayInfoHead/DayInfoHead';

const DayInfo = () => {
  return (
    <section className="dayInfo">
      <DayInfoHead />
    </section>
  );
};

export default DayInfo;
