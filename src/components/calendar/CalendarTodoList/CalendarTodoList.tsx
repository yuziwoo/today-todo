import { CalendarDateProps } from '../../../types/calendar';

interface CalendarTodoListProps {
  date: CalendarDateProps;
}

const CalendarTodoList = ({ date }: CalendarTodoListProps) => {
  return <div>dd</div>;
};

export default CalendarTodoList;
